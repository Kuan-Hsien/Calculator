<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar" >
        <item name="android:windowNoTitle">true</item>
        <item name="colorPrimary">@color/background_calculator_grey</item>
        <item name="colorPrimaryDark">@color/background_calculator_grey</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorBackground">@color/background_calculator_grey</item>
    </style>

    <style name="ImageButtonStyle" parent="android:TextAppearance" >
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:adjustViewBounds">true</item>
        <item name="android:background">#00000000</item>
        <item name="android:cropToPadding">true</item>
        <item name="android:scaleType">centerInside</item>
    </style>

</resources>
